services:
  qdrant:
    image: qdrant/qdrant:v1.13.4
    container_name: lab-qdrant
    restart: unless-stopped
    volumes:
      - qdrant_data:/qdrant/storage
    ports:
      - "127.0.0.1:6333:6333"

volumes:
  qdrant_data:
